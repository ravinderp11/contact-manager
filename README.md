# Contact Manager

A simple command-line app in Python to manage a list of contacts.

## Features

- Add new contacts
- List all contacts
- Search contacts
- Save contacts to file

## How to Run

Make sure Python is installed, then:

```bash
python3 contacts.py

elif choice == "search":
    name = input("Enter name to search: ")
    search_contacts(name)
